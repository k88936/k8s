apiVersion: v1
kind: ConfigMap
metadata:
  name: teamcity-agent-config
  namespace: teamcity
data:
  SERVER_URL: "http://teamcity.teamcity:8111"
  NO_PROXY: "localhost,127.0.0.1,::1,.local"
  HTTP_PROXY: "http://wallence.wallence.svc.cluster.local:7890"
  HTTPS_PROXY: "http://wallence.wallence.svc.cluster.local:7890"
---
apiVersion: v1
kind: Service
metadata:
  name: teamcity-agent-headless
  namespace: teamcity
  labels:
    app: teamcity-agent
spec:
  clusterIP: None
  selector:
    app: teamcity-agent
  ports:
    - name: agent
      port: 9090
      targetPort: 9090


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: teamcity-agent
  labels:
    app: teamcity-agent
  namespace: teamcity
spec:
  serviceName: teamcity-agent-headless
  selector:
    matchLabels:
      app: teamcity-agent
  template:
    metadata:
      labels:
        app: teamcity-agent
    spec:
      containers:
        - name: app
          image: kvtodev/teamcity-agent:latest
          imagePullPolicy: Always
          securityContext:
            privileged: true
          envFrom:
            - configMapRef:
                name: teamcity-agent-config
          volumeMounts:
            - name: cache
              mountPath: /cache
              subPath: cache
            - name: cache
              mountPath: /var/lib/containers
              subPath: podman
            - name: conf
              mountPath: /data/conf
  volumeClaimTemplates:
    - metadata:
        name: cache
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-storage
        resources:
          requests:
            storage: 128Gi
        selector:
          matchLabels:
            type: teamcity-agent-cache
    - metadata:
        name: conf
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: longhorn
        resources:
          requests:
            storage: 128Mi
